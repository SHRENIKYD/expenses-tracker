{
  "name": "expenses-tracker",
  "version": "1.1.0",
  "private": true,
  "description": "Tessera \u2014 personal expenses tracker (React on GitHub Pages, Supabase behind it)",
  "scripts": {
    "install:all": "npm install && npm --prefix client install && npm --prefix supabase install",
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "test": "npm --prefix client test && npm --prefix supabase test"
  }
}
