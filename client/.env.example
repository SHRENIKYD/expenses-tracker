# The Supabase project the client talks to. Both values come from the project's
# API settings; the publishable key ships in the browser bundle, which is safe
# only because every table has row-level security.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
